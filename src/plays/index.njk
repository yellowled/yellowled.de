---
title: Was ich spiele
description: Meine (noch überschaubare) Instrumentensammlung
layout: page.njk
pagination:
    data: instruments
    size: 10
---
<p>2021 habe ich mir nach rund 30 Jahren Pause wieder eine elektrische Gitarre zugelegt (und dann gleich noch eine). Hier also meine (derzeit noch überschaubare) Instrumentensammlung.</p>

<ul>
{% for instrument in instruments %}
    <li><a href="{{ instrument.model | slug }}/">{{ instrument.brand }} {{ instrument.model }}</a></li>
{% endfor %}
</ul>
{% if pagination.href.previous or pagination.href.next %}
<nav class="pagination" aria-label="Pagination">
    <ul>
        <li>{% if pagination.href.previous %}<a class="pagination__prev" href="{{ pagination.href.previous }}">Neuere Einträge</a>{% endif %}</li>
        <li>{% if pagination.href.next %}<a class="pagination__next" href="{{ pagination.href.next }}">Ältere Einträge</a>{% endif %}</li>
    </ul>
</nav>
{% endif %}
