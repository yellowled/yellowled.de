<script is:inline>
    const stored = window.localStorage.getItem('color-mode');
    if (typeof stored === 'string') {
        document.documentElement.setAttribute('data-force-color-mode', stored);
    }
</script>
