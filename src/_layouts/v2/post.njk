---
permalink: 'archiv/{{ id }}/{{ slug }}.html'
---
<!DOCTYPE html>
<html lang="{{ meta.lang }}" class="no-js">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>{{ title or meta.title }}</title>
    {% include "meta.njk" %}
    {% include "favicons.njk" %}
    <link rel="stylesheet" href="{{ '/css/main.css' | url }}">
</head>
<body>
    <main>
        <article>
            <footer>
                <p>Dieser Artikel ist archiviert. Er wurde am <time datetime="{{ date | dateISO }}">{{ date | dateFormat }}</time> in <i>YellowLeds Weblog v2</i> veröffentlicht. Der Inhalt ist inzwischen wahrscheinlich veraltet. Kommentare und Trackbacks wurden entfernt.</p>

                <a href="/archiv/">Zur Archivübersicht</a>
            </footer>

            <h1>{{ title }}</h1>

            {{ content | safe }}
        </article>
    </main>
</body>
</html>
